default:
  level: info
  encoding: console
  consoleEncodingConfig:
    partsOrder: [level, message]
    disableTimestamp: true
    disableColor: true
  handler:
    type: file
    fileConfig:
      path: ""