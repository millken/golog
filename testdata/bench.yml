default:
  level: info
  encoding: text
  textEncodingConfig:
    partsOrder: [level, message]
    disableTimestamp: true
    disableColor: true
  handler:
    type: file
    fileConfig:
      path: ""