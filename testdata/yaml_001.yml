default:
  level: info
  encoding: text
  callerLevels: [debug, info, warn, error, fatal, panic]
  stacktraceLevels: [warn, error, fatal, panic]
  handler:
    type: file
    file:
      path: /var/log/golog.log
modules:
  mudule/1:
    level: debug
    encoding: json